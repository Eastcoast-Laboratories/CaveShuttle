# TODO

- [ ] sterne sollen leicht flackern
- [ ] man muss mit dem podhalter kollidieren können
- [ ] Solange der Schild aktiv ist, kannst du nicht schießen
- [x] Classic-Levels nach `/var/www/thrust-levels` ausgelagert und eigenes Git-Repo https://github.com/rubo77/thrust-classic-levelpack angelegt (`meta.json` + `level1.def`…`level6.def`).
- [ ] der editor die zufällige hügelige oberfläche, die aufsteigende und absteigende boden-hügelkette muss immer wenn die Richtung wechselt Modetrends einmal auf der selben Höhe bleiben um rechtzeitig das slope elements to zeichnen, das die umgekehrte Richtung einleitet
- [ ] explosive obstacles
- [ ] Vibration off toggle geht nicht, es vibriert trotzdem bei explosion
- [ ] beim ersten app start wird ja ein playername generiert dieser soll für immer in der localstorage gespeichrt bleiben als  eine eindeutige User-ID für später das syncen mit dem onlinedienst auf community.caveshuttle.z11.de . der soll auch mit exportiert werden beim mdata-export und nur beim data-impoort kann er überschrieben werden, oder wenn man alle daten löscht.
- [ ] nie levels aus verschiedenen lpacks gleichzeitig auf einer seite gelistet sein, die levelpacks sind die obere unterscheidung, innerhalb eines level packs gibt es nur die Highscores für run und level-ids. das muss immer die oberste auswahl sein, default ist das default-levelpack ausgewählt, aber die anderen können umbeschaltet werden, dannn kommt eine komplett andere highscore für nur dieses levelpack. Level 1 aus einem pack ist überhaupt nicht vergleichbar oder competitierbar mit einem level 1 aus einem anderen pack
